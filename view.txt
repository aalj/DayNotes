1. canvas 位移
2. paint边缘锯齿的处理。
3. canvas缩放大于“1”是放大，小于“1”是缩小，为负数表示翻转
	1. 缩放指的是canvas的缩放，就是说在放大或缩小页面的时候对应的线条都会进行放大和缩小
4. 旋转试用方法和缩放及其相似，也是两个方法，一个是以原点作为缩放中心，一个是以设置的点作为缩放中心
5. 错切可以理解为根据正切值进行数值转换，然后变化canvas进行图形变换
	1. X = x+sx*y；
	2. Y = sy*x+y；
